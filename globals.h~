/*
Parikh Shivam
ICSI 402
Assignment 4
UNIX ID: sp191221
*/     
        
 int lineCount = 0; //Line Counter
 int lcValue = 0;   //Location Counter
 
 int lcAssembly = 0;
        
 struct symbolNode *head = NULL;  // head pointer for symbol table linked list
 struct symbolNode *temp = NULL;  // temp pointer for symbol table linked list
 
 struct illegalOpcode *illegalHead = NULL;     // head pointer for illegal opcode linked list
 struct illegalOpcode *illegalTemp = NULL;     // temp pointer for illegal opcode linked list
 
 struct undefinedSymbol *undefinedHead = NULL;
 struct undefinedSymbol *undefinedTemp = NULL;
               
 struct opCodeTable opArray[OPCODEARRAY]; //opcode table is size of 36

 int assembly[ARRAYSIZE]; // after assembly process the value will be stored on to array
 int location = 0; // for the assembly array index

 
